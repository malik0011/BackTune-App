package com.malikstudios.backtune.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// Modern Relaxing Color Palette
object BackTuneColors {
    // Primary Colors
    val Primary = Color(0xFF7B68EE)  // Medium Slate Blue
    val PrimaryVariant = Color(0xFF6A5ACD)  // Slate Blue
    val Secondary = Color(0xFF20B2AA)  // Light Sea Green

    // Background Colors
    val Background = Color(0xFF121212)  // Dark Background
    val Surface = Color(0xFF1E1E1E)  // Slightly Lighter Dark
    val CardBackground = Color(0xFF2D2D2D)  // Card Background

    // Text Colors
    val TextPrimary = Color(0xFFE0E0E0)  // Light Gray
    val TextSecondary = Color(0xFFB0B0B0)  // Medium Gray
    val TextTertiary = Color(0xFF808080)  // Dark Gray

    // Accent Colors
    val AccentGreen = Color(0xFF4CAF50)  // Success Green
    val AccentRed = Color(0xFFE57373)  // Soft Red
    val AccentYellow = Color(0xFFFFB74D)  // Warm Yellow

    // Gradients
    val PrimaryGradient = listOf(
        Color(0xFF7B68EE),  // Medium Slate Blue
        Color(0xFF6A5ACD)   // Slate Blue
    )

    val SecondaryGradient = listOf(
        Color(0xFF20B2AA),  // Light Sea Green
        Color(0xFF4CAF50)   // Success Green
    )
}

// Primary Colors
val SoftTealStart = Color(0xFF77DACC)
val SoftTealEnd = Color(0xFF64B5F6)
val SoftPurple = Color(0xFFC5A3FF)

// Background Colors
val LightCream = Color(0xFFFAF9F6)

// Text Colors
val TextPrimary = Color(0xFF2F3C4A)
val TextSecondary = Color(0xFF7B8B99)

// Action Colors
val SuccessGreen = Color(0xFF5DC287)
val ErrorRed = Color(0xFFF28B82)

// Gradients
val PrimaryGradient = listOf(SoftTealStart, SoftTealEnd)

// Light Theme Colors
val Primary = Color(0xFF6200EE)
val PrimaryVariant = Color(0xFF3700B3)
val Secondary = Color(0xFF03DAC6)
val SecondaryVariant = Color(0xFF018786)

// Dark Theme Colors
val DarkPrimary = Color(0xFFBB86FC)
val DarkPrimaryVariant = Color(0xFF3700B3)
val DarkSecondary = Color(0xFF03DAC6)
val DarkSecondaryVariant = Color(0xFF03DAC6)

// Common Colors
val Error = Color(0xFFB00020)
val OnPrimary = Color(0xFFFFFFFF)
val OnSecondary = Color(0xFF000000)
val OnBackground = Color(0xFF1C1B1F)
val OnSurface = Color(0xFF1C1B1F)
val OnError = Color(0xFFFFFFFF)